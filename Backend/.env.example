# Backend Environment Configuration - PRODUCTION (Railway)
# Copy this file to .env and fill in your actual values from Supabase

# Supabase Configuration (Required - from Supabase Dashboard)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-anon-key-from-settings
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-from-settings

# Frontend URL Configuration (Required for CORS - where your frontend is deployed)
# GitHub Pages example: https://carzown.github.io/Glass-Defect-Detection-Prototype
FRONTEND_URL=https://your-frontend-url.com

# Server Configuration
PORT=5000
NODE_ENV=production

# Security (MUST be changed to a strong random value!)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_TOKEN=your-strong-random-token-here
